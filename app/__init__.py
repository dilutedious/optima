"""Optima — Automated Study Flow.

v0.3 package. Modules:
- auth.py     — password hashing + validators
- storage.py  — JSON load/save (atomic in v0.3.1)
- models.py   — dataclasses for the domain objects
- priority.py — priority score
- scheduler.py — greedy schedule generator
- main.py     — Flask app factory + routes
"""

__version__ = "0.3.0"
